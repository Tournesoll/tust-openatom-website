# 快速开始指南

## 🚀 5 分钟快速上手

### 步骤 1：安装 Node.js

确保已安装 Node.js（版本 14+）：
```bash
node --version
```

如果没有，访问：https://nodejs.org/

### 步骤 2：安装依赖

```bash
cd admin
npm install
```

### 步骤 3：配置 GitHub OAuth

1. **复制配置文件**
   ```bash
   cp config.example.json config.json
   ```

2. **编辑 `config.json`**
   ```json
   {
     "github": {
       "clientId": "Ov23li4O8m6iO9mGQK4P",
       "clientSecret": "576ae9e34cd949ef000e5b88005479f1f865d3a5",
       "repo": "Tournesoll/tust-openatom-website",
       "branch": "main"
     },
     "server": {
       "port": 8888,
       "sessionSecret": "请修改为随机字符串"
     },
     "paths": {
       "postsDir": "../_posts",
       "imagesDir": "../assets/images/uploads"
     }
   }
   ```

3. **更新 GitHub OAuth App 回调 URL**
   - 访问：https://github.com/settings/developers
   - 找到你的 OAuth App
   - 更新回调 URL 为：`http://localhost:8888/api/auth/github/callback`

### 步骤 4：启动服务器

```bash
npm start
```

### 步骤 5：访问管理后台

打开浏览器访问：`http://localhost:8888`

## 📝 使用说明

### 登录
1. 点击 "使用 GitHub 登录"
2. 授权访问仓库
3. 登录成功

### 管理文章

**查看文章列表**
- 自动显示所有 `_posts/` 目录下的文章

**创建文章**
1. 点击 "新建文章"
2. 填写文章信息
3. 编写 Markdown 内容
4. 点击 "保存" 或 "保存并推送"

**编辑文章**
1. 在文章列表中点击 "编辑"
2. 修改内容
3. 保存

**删除文章**
1. 在文章列表中点击 "删除"
2. 确认删除
3. 自动提交到 Git

### Git 操作

**拉取更新**
- 点击 "拉取更新" 按钮
- 自动执行 `git pull origin main`

**保存并推送**
- 创建/编辑文章后，点击 "保存并推送"
- 自动执行：
  - `git add .`
  - `git commit -m "消息"`
  - `git push origin main`

## ⚠️ 注意事项

1. **确保 Git 已配置**
   ```bash
   git config --global user.name "Your Name"
   git config --global user.email "your.email@example.com"
   ```

2. **确保有 Git 远程仓库**
   ```bash
   git remote -v
   # 应该显示 origin 指向你的 GitHub 仓库
   ```

3. **首次使用需要拉取**
   - 启动后先点击 "拉取更新"
   - 确保本地仓库是最新的

## 🐛 常见问题

### 问题：登录后提示 401 Unauthorized
**解决**：检查 `config.json` 中的 Client ID 和 Secret 是否正确

### 问题：Git 操作失败
**解决**：
- 检查 Git 是否已配置
- 检查是否有远程仓库
- 检查是否有推送权限

### 问题：文章列表为空
**解决**：
- 检查 `_posts/` 目录是否存在
- 检查 `config.json` 中的 `paths.postsDir` 是否正确

### 问题：端口被占用
**解决**：修改 `config.json` 中的 `server.port`

## 🎉 完成！

现在你可以通过可视化界面管理 Jekyll 文章了！




